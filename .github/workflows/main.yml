name: My Workflow
on: [ push, pull_request ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: RUN 
        run : echo "testing"
      - name: RUNfile 
        run : echo "testing something"
